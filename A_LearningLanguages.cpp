#include<bits/stdc++.h>
using namespace std;

#define ll long long int
#define fastio ios_base::sync_with_stdio(false),cin.tie(NULL);

int main()
{	
	fastio
	ll n,m;
	cin >> n>>m;
	for (int i = 0; i < n; ++i){
		ll k;
		cin>>k;
		ll a[k];
		for (int j = 0; j < k; ++j){
			cin>>a[j];
		}
	}
	for (int i = 0; i < n; ++i){
	 	
	 } 
	return 0;
}