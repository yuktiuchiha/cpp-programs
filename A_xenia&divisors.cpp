#include<bits/stdc++.h>
using namespace std;

#define ll long long int

int main()
{
	ll n;
	cin >> n;
	int a[n];
	for (int i = 0; i < n; ++i){
		cin >> a[i];
	}
	sort(a,a+n);
	vector<int> v1;
	vector<int> v2;
	v1.push_back(a[0]);
	for (int i = 1; i < n; ++i){
		
		}
	}
	return 0;
}